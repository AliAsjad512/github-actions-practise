{
  "name": "ci-project",
  "version": "1.0.0",
  "scripts": {
    "test": "jest",
    "lint": "eslint src/**/*.js"
  },
  "jest": {
    "testMatch": ["**/tests/**/*.test.js"]
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "jest": "^29.0.0"
  }
}
